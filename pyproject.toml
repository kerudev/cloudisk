[project]
name = "cloudisk"
version = "0.1.0"
description = ""
readme = "README.md"
license = { text = "" }
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.121.0",
    "sqlalchemy>=2.0.44",
    "uvicorn>=0.38.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.3",
]
