# cloudisk

## Dependencies

Tools:
- Python 3.11
- uv

Libraries:
- fastapi >= 0.121.0
- sqlalchemy >= 2.0.44

Dev libraries:
- pytest >= 8.4.2
- pytest-cov >= 7.0.0
- ruff >= 0.14.3

## Install deps

To install the dependencies in `pyproject.toml`, run:

```sh
uv sync
```
